interface, takový aby bylo možné jen změnit jméno, a jel tam jakýkoliv jiný pohybový senzor, tzn nejdříve vytvořit něco generického:

interface ZilogC {
  command void init(); //mozna

  //uspani a probuzeni senzoru / cidla (u toho druheho to bude potreba implementovat primo HW
  command void suspend();
  command void wakeUp();
  command bool isSuspended();

  //spusteni detekce pohybu  (pokud byl senzor pred tim uspany, tak by bylo dobre zadat jeste nejake cekani, aby to uzivatel modulu
  //nezkonil a nesnazil se skenovat s podstate jeste spicim cidlem
  command void scan();
  command bool isScanning();

  //vyvolani udalosti, aby mohl uzivatel patricne reagovat (nejspis by bylo vhodne nějak navracet hodnoty toho co se stalo př:
  // enum { ERROR = 0, MOVE_Y, MOVE_N }; 
  //nebo misto navratove hodnoty pridat command, ktery vrati informaci, nejsem si momentalne jist jestli muze event neco vracet
  //command uint8_t scanResult();
  event void scanDone();
} 
